# TradeVision End-to-End and Integration Tests

Sprint 0 provides backend unit/integration tests in `backend/tests/`.
This directory is reserved for workspace-level end-to-end integration tests as the project progresses through subsequent sprints.
